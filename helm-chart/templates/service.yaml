  apiVersion: v1
kind: Service
metadata:
  name: {{ include "dns-manager.fullname" . }}
  labels:
    {{- include "dns-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dns-manager.selectorLabels" . | nindent 4 }} 